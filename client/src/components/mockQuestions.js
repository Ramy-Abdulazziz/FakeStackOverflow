const tag = {
    name: "javascript"

 }
const a = {
    title: "Programmatically 1 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  const b = {
    title: "Programmatically 2 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const c = {
    title: "Programmatically 3 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const d = {
    title: "Programmatically 4 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const e = {
    title: "Programmatically 5 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const f = {
    title: "Programmatically 6 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const g = {
    title: "Programmatically 7 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag, tag, tag, tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const h = {
    title: "Programmatically 8 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag, tag, tag, tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };

  
  const i = {
    title: "Programmatically 9 using React router",
    text: "the alert shows the proper index for the li clicked, and when I alert the variable within the last function I'm calling, moveToNextImage(stepClicked), the same value shows but the animation isn't happening. This works many other ways, but I'm trying to pass the index value of the list item clicked to use for the math to calculate.",
    summary:"need to learn how to use react better",
    tags:[tag, tag, tag, tag],
    answers:[], 
    comments:[], 
    asked_by:"jolly jon",
    ask_date:"May 25th, 2023",
    views: 25,
    upvotes: 10
  };


  export default class MockQuestions{

    static getQuestions(){

        return [a,b,c,d,e,f,g,h,i]
    }
  }